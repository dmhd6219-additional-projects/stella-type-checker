language core;

extend with #nested-function-declarations;

fn main(n : Nat) -> Nat {

    fn increment_twice(p : Nat) -> (fn (Nat) -> Nat) {
        return fn(m : Nat, g : Nat) {
            return succ(m)
        }
    }

    return increment_twice(0)(0)
}
