language core;

fn noop(_ : {}) -> {} {
  return {}
}

fn unwrap(t : {Nat, Nat, Nat}) -> {Nat, Nat} {
  return {t.1, t.2}
}

fn main(n : Nat) -> Nat {
  return unwrap({n, succ(n), succ(succ(n))}).1
}
